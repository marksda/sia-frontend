import { Button, ButtonProps } from "@fluentui/react-components";

const PortraitHomeScreen = (props: ButtonProps) => {
    return <Button {...props}>Portrait</Button>;
};

export default PortraitHomeScreen;